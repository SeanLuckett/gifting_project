%h4 Please confirm your email address.
= form_for @user, :url => static_pages_send_and_confirm_path, :html => { :class => 'form-inline' } do |f|
  = f.text_field :email
  = f.submit "Confirm", :class => 'btn'
